<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinUIApp.Views.Pages.DrinkDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUIApp.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:components="using:WinUIApp.Views.Components"
    xmlns:votebutton="using:WinUIApp.Views.Components"
    xmlns:modals="using:WinUIApp.Views.Components.Modals"
    mc:Ignorable="d"
    Background="{ThemeResource SolidBackgroundFillColorBaseBrush}">

    <Grid
        HorizontalAlignment="Stretch"
           Margin="32,0,32,0">
        <ScrollViewer>
            <StackPanel Orientation="Vertical">
                <StackPanel Orientation="Horizontal">

                    <Image Source="https://picsum.photos/id/237/200/300"
               x:Name="DrinkImage"
               Height="180"/>

                    <StackPanel Orientation="Vertical"
                   Margin="24,0,0,0">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" >
                            <Border 
                        Width="48" 
                        Height="48"
                        Background="{ThemeResource AccentFillColorSecondaryBrush}" 
                        CornerRadius="8" 
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                        BorderThickness="1" 
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Center">
                            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock FontSize="16"  TextAlignment="Center" FontWeight="Bold" Foreground="{ThemeResource TextFillColorPrimaryBrush}">
                            <Run Text="{Binding AverageReviewScore}" />
                            <Run Text="/ 5" />
                                </TextBlock>
                            </Grid>
                        </Border>
                        <TextBlock Text="{Binding Drink.DrinkName}"
                   HorizontalAlignment="Center"
                   x:Name="Name" 
                   FontSize="26" 
                   Margin="8,0,0,0"
                   FontWeight="Bold" 
                   Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 5 0 0">
                        <components:AddRemoveFromDrinkListButton DrinkId="{Binding Drink}" />

                            <components:UpdateDrinkButton x:Name="UpdateButton" Drink="{Binding Drink}" />
                            <Button>
                            <Button.Flyout>
                                <Flyout Placement="Bottom">
                                    <StackPanel>
                                        <TextBlock Text="Are you sure?" HorizontalAlignment="Center" />
                                        <Button x:Name="ConfirmRemoveButton" Click="ConfirmRemoveButton_Click" HorizontalAlignment="Center" Margin="0 5 0 0">Confirm</Button>
                                    </StackPanel>
                                </Flyout>
                            </Button.Flyout>
                            <TextBlock x:Name="RemoveButtonText" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                        </Button>
                        <votebutton:VoteDrinkOfTheDayButton Tapped="VoteButton_Click"  DrinkId="{Binding DrinkDTO.DrinkId}" />

                    </StackPanel>
                    </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch">
                    <TextBlock Text="Brand" FontSize="18" FontWeight="Bold" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    <TextBlock Text="{Binding Drink.DrinkBrand.BrandName}" FontSize="14" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    <TextBlock Text="Alcohol percentage" FontSize="18" FontWeight="Bold" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    <TextBlock Text="{Binding Drink.AlcoholContent}" FontSize="14" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    <TextBlock Text="Categories" FontSize="18" FontWeight="Bold" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    <TextBlock Text="{Binding CategoriesDisplay}" FontSize="14" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    <Grid  Margin="0,16,0,0" HorizontalAlignment="Stretch" Width="Auto">
                        <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal" Grid.Column="0">
                            <FontIcon Glyph="&#xE90A;" Margin="0,0,8,0"/>
                            <TextBlock Text="Reviews" FontSize="24" FontWeight="Bold" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                        </StackPanel>

                        <Button Content="Add Review" Grid.Column="1" HorizontalAlignment="Right"
                            Command="{x:Bind ViewModel.ShowPopupCommand, Mode=OneWay}"  Style="{StaticResource AccentButtonStyle}"
                            Margin="5,0" />
                    </Grid>
                    <Rectangle Height="1" Fill="{ThemeResource TextFillColorPrimaryBrush}" HorizontalAlignment="Stretch"
                               Margin="0,8,0,16"/>
                    
                    <ItemsControl ItemsSource="{Binding Reviews}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Padding="10" Margin="0 0 0 10" 
                        CornerRadius="8" >
                                    <StackPanel>
                                        <TextBlock Text="{Binding Content}" Foreground="{ThemeResource TextFillColorPrimaryBrush}" TextWrapping="WrapWholeWords"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                </StackPanel>
            </StackPanel>
        </ScrollViewer>
        <!-- add review modal -->
        <Grid x:Name="AddReviewModalOverlay"
            Visibility="Collapsed"          
               Background="#80000000"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Canvas.ZIndex="999">
            <modals:AddReviewModal x:Name="AddReviewModal"
                                   CloseRequested="CloseAddReviewModal" DrinkId="{Binding Drink.DrinkId}"/>
        </Grid>
    </Grid>
</Page>